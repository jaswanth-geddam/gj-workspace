# project-runner

Management scripts for GJ's 15+ project portfolio under `C:\code\coaching\gj\`.

- `run.bat` — interactive menu: install all, lint/format, run any project
- `gj.code-workspace` — VS Code workspace that opens all sibling project folders
